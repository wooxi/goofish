# Frontend dev server
VITE_DEV_SERVER_PORT=8002
VITE_DEV_API_PROXY_TARGET=http://localhost:8001

# Backend base URL for browser requests.
# - empty => same origin (recommended with Nginx /api reverse proxy)
# - set to explicit origin in dev, e.g. http://localhost:8001
VITE_API_BASE_URL=
